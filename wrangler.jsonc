{
  // Static-assets-only Worker: serves the hand-coded landing from the repo root.
  // No Worker script, no build step. Deployed via `npx wrangler deploy`.
  "name": "quote-calculator-landing",
  "compatibility_date": "2025-06-01",
  "assets": {
    "directory": "./"
  }
}
